# Environment configuration for development
#
# SESSION_SECRET is used by express-session. Set a strong value in production.
# PORT is optional and defaults to 3000 if not provided.

SESSION_SECRET=your_dev_secret
PORT=3000
